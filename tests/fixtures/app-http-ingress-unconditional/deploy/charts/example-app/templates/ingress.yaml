apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-app
spec:
  tls:
    - hosts:
        - "{{ .Values.cloudDomain }}"
      secretName: "{{ .Values.certSecretName }}"
  rules:
    - host: "{{ .Values.cloudDomain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: example-app
                port:
                  number: 80
